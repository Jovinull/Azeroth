package main;

/**
 * Enum que representa as ações de controle do jogo.
 * Permite mapear comandos como movimentação, ataque, pulo, etc.
 */
public enum GameAction {
    MOVE_UP,
    MOVE_DOWN,
    MOVE_LEFT,
    MOVE_RIGHT
}
